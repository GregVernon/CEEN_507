function [U,ELEM] = AssembleSolution6DOF(numElem, ELEM, BSpline, ECONN)
%iterate to assemble global stiffness and force matrices from element matrices
ndof = 6; % 6 Degrees of Freedom
for a = 1:numElem
  for idof = 1:ndof
    if K[e,a,idof]

end
